name: Hello World
on: workflow_dispatch
jobs:
  Hello-World-Job:
        runs-on: ubuntu-latest
        steps:
          - name: Hello-World-Step
          run: echo "Hello World, Welcome to GHA"
          
  
